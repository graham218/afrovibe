<!DOCTYPE html>
<html lang="en" data-theme="afrovibe">
  <%- include('partials/head', { pageTitle: (pageTitle || 'About') }) %>
<body class="bg-black text-white min-h-screen flex flex-col">
  <%- include('partials/navbar', { currentUser }) %>

  <main class="relative isolation-isolate z-10 max-w-5xl mx-auto px-4 md:px-6 lg:px-8 py-10 flex-1">
    <div aria-hidden="true" class="pointer-events-none absolute inset-0 -z-10
         bg-[radial-gradient(60%_50%_at_50%_0%,rgba(255,107,107,.12),transparent_60%),radial-gradient(60%_40%_at_80%_100%,rgba(254,202,87,.10),transparent_60%)]"></div>

    <section class="rounded-3xl bg-black/60 backdrop-blur-xl border border-white/10 p-6 md:p-8 shadow-[0_12px_40px_-12px_rgba(0,0,0,.6)]">
      <h1 class="text-3xl md:text-4xl font-bold bg-gradient-to-r from-[#FF6B6B] to-[#FECA57] bg-clip-text text-transparent">
        About AfroVibe
      </h1>
      <p class="mt-4 text-white/80 leading-relaxed">
        AfroVibe helps you find real connections with intent. Smart filters, safe tools, and a warm community vibe.
      </p>
      <ul class="mt-6 grid sm:grid-cols-2 gap-4">
        <li class="p-4 rounded-2xl bg-black/50 border border-white/10">💬 Real conversations</li>
        <li class="p-4 rounded-2xl bg-black/50 border border-white/10">🔒 Safety & verification</li>
        <li class="p-4 rounded-2xl bg-black/50 border border-white/10">⚡ Boosts & smart discovery</li>
        <li class="p-4 rounded-2xl bg-black/50 border border-white/10">🌍 International community</li>
      </ul>
    </section>
  </main>

  <%- include('partials/footer') %>
</body>
</html>
